// tailwind.config.ts (ESM 写法)
import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: "media", // 或 "class"
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
